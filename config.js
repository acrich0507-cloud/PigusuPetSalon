const CONFIG = {
    // 前端部署版本（每次發布請改這裡，用來確認 LIFF / 靜態檔是否已更新）
    APP_VERSION: "2026.09.16.1",

    // Cloud Run API 網址（沿用既有欄位名稱以保持前端相容）
    GAS_API_URL: "https://pigusu-backend-2697294314.asia-east1.run.app/exec",
    
    // 你的 LINE LIFF ID (建議一併抽離，因為首頁跟其他頁面可能都會用到)
    LIFF_ID: "2011433016-ReLRLT5q" 

};
